import { StyleSheet } from 'react-native';

const calender = StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: 'column',
        justifyContent: 'flex-start',
        alignItems: 'center',
        marginTop: '15%',
    },

    textLeft: {
        flex: 1,
        justifyContent: 'flex-start',
        alignItems: 'flex-start',
    }
});

export default calender;