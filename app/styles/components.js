import { StyleSheet } from 'react-native';

const components = StyleSheet.create({
    button: {
        marginTop: 10,
        padding: 20,
        width: '45%',
        borderColor: '#F1F1F1',
        borderTopWidth: 2,
        alignItems: 'center',
    },

    buttonLabel: {
        color: '#F1F1F1',
        fontWeight: '700',
        fontSize: 18
    }
});

export default components;